# Stock Dashboard 개발 프로젝트 지침

## 1. 프로젝트 목표
- GitHub 저장소 `poormanZ/stock`에서 웹 기반 주식 대시보드를 개발한다.
- 여러 주식 종목을 하나의 화면에서 동시에 확인할 수 있는 것이 핵심 목표다.
- 초기 버전은 개인/소규모 사용을 전제로 하며, 실제 매매 주문 기능은 범위에서 제외한다.
- GitHub Actions와 GitHub Pages를 활용해 빌드 및 정적 웹 배포가 가능한 구조를 우선한다.

## 2. 작업 원칙
- 모든 답변과 문서는 한국어로 작성한다.
- 작업 시작 전에 반드시 원격 `main`의 최신 상태와 최신 commit SHA를 확인한다.
- 파일을 실제 반영하기 직전에도 `main` 최신 SHA를 다시 확인한다.
- 작업 기준 SHA와 최신 SHA가 다르면 변경사항을 다시 동기화한 뒤 작업한다.
- 기존 파일/구조를 먼저 확인하고 필요한 최소 변경만 적용한다.
- 기능을 한 번에 크게 만들지 않고 설계 → 최소 구현 → 검증 → 다음 기능 순으로 진행한다.
- 구현하지 않은 기능을 완료된 것으로 문서화하지 않는다.
- 금융 데이터는 출처와 갱신 시점을 명확히 표시하며, 데이터 지연 가능성을 UI에서 숨기지 않는다.

## 3. 기술 방향
- 프론트엔드는 유지보수성이 높은 현대적인 웹 기술을 사용한다.
- 초기에는 정적 배포를 우선 고려하고, 브라우저에서 직접 호출하기 어려운 데이터 공급자는 향후 서버리스/API 계층을 별도로 검토한다.
- 데이터 공급자 API 키를 프론트엔드 코드나 Git 저장소에 하드코딩하지 않는다.
- 종목 데이터 모델과 UI를 분리해 데이터 공급자를 교체할 수 있도록 한다.
- 시간대와 시장별 거래시간을 고려하되, 초기 MVP에서는 명확한 기준 시간대를 사용하고 확장 가능하게 설계한다.

## 4. 핵심 제품 원칙
- 한 화면에서 여러 종목을 빠르게 비교할 수 있어야 한다.
- 숫자만 나열하기보다 현재가, 등락, 등락률, 거래량 등 핵심 정보를 우선한다.
- 관심종목 추가/삭제와 화면 배치가 단순해야 한다.
- 데스크톱을 우선하되 모바일에서도 기본적인 확인이 가능하도록 반응형으로 설계한다.
- 실시간 데이터처럼 보이게 오해를 유발하지 않는다. 데이터 시각과 지연 여부를 표시한다.

## 5. 단계별 개발
1. 프로젝트 지침 및 설계 문서 확정
2. 웹 프로젝트 기본 구조 및 GitHub Actions/Pages 배포 기반 구축
3. 대시보드 레이아웃과 샘플 데이터 구현
4. 종목 추가/삭제 및 관심종목 저장
5. 실제 주가 데이터 연동
6. 차트 및 기간별 데이터
7. 시장/종목 필터와 정렬
8. 오류/로딩/데이터 지연 처리 및 성능 개선
9. 테스트, 접근성, 모바일 대응 및 배포 검증

## 6. Git 규칙
- 의미가 명확한 작은 단위로 커밋한다.
- 문서 변경과 기능 변경을 가능한 한 구분한다.
- `main`에 직접 대규모 실험 코드를 누적하지 않는다. 필요하면 작업 브랜치와 PR을 사용한다.
- 커밋 전 최신 `main` SHA를 재확인한다.

## 7. 완료 기준
각 단계는 다음을 확인한 후 완료 처리한다.
- 코드/문서가 저장소에 반영됨
- 빌드 또는 정적 검증 통과
- 주요 동작을 실제 브라우저에서 확인
- 관련 문서 업데이트
- 다음 단계에서 필요한 기술적 전제와 남은 과제를 기록
