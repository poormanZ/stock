# 한국투자증권 자동 주식매매 프로젝트 지침

## 1. 프로젝트 목표
- GitHub 저장소 `poormanZ/stock`을 한국투자증권(KIS) API 기반의 웹 주식 대시보드 및 자동매매 시스템으로 개발한다.
- 핵심 목표는 **시장 데이터 조회 → 전략 판단 → 주문 생성 → 주문 전송 → 체결 확인 → 포지션/잔고 반영 → 거래 기록**의 전체 흐름을 안정적으로 관리하는 것이다.
- 문서와 코드는 항상 저장소의 최신 상태를 기준으로 함께 관리한다.
- 초기 개발은 모의/드라이런을 우선하며, 실계좌 주문은 명시적인 설정과 안전장치를 모두 통과한 경우에만 허용한다.

## 2. 최우선 원칙
- 모든 답변, 설계 문서, 주석, 커밋 메시지는 한국어를 기본으로 한다. 코드의 식별자와 외부 API 고유 명칭은 원문을 유지할 수 있다.
- 작업 시작 전에 반드시 원격 `main`의 최신 commit SHA를 확인한다.
- 작업 중 GitHub에 변경을 반영하기 직전에도 `main` 최신 SHA를 다시 확인한다.
- 작업 기준 SHA와 최신 SHA가 다르면 최신 `main`을 다시 기준으로 삼아 변경사항을 재검토한 뒤 반영한다.
- 기존 코드와 문서 구조를 먼저 확인하고, 필요한 최소 범위로 변경한다.
- 구현되지 않은 기능을 완료된 것으로 표시하지 않는다.
- 문서의 상태와 실제 코드의 상태가 다르면 코드를 임의로 완료 처리하지 말고 문서를 실제 상태에 맞춘다.
- 금융/주문 관련 기능은 정확성·안전성·추적 가능성을 UI 편의성보다 우선한다.

## 3. 자동매매 안전 원칙
- **기본 실행 모드는 DRY_RUN 또는 모의투자**로 한다.
- 실계좌 주문 기능은 기본값으로 비활성화하며, 별도의 명시적 활성화 설정 없이는 주문 API를 호출하지 않는다.
- 실계좌 전환 시 계좌 환경, API 인증정보, 주문 가능 여부, 장 운영시간, 종목/수량/가격 검증을 각각 확인한다.
- 주문 전 최대 주문금액, 최대 보유비중, 종목별 최대 수량, 일일 최대 손실/주문 횟수 등 위험 제한을 둘 수 있는 구조로 만든다.
- 비정상 상태, API 오류, 인증 실패, 시세 지연, 잔고 불일치, 주문 상태 불명확 시 신규 주문을 중단하는 fail-safe를 우선한다.
- 긴급 정지(kill switch)를 구현할 수 있도록 모든 주문 경로를 중앙화한다.
- 동일 주문의 중복 전송을 방지하고, 주문 요청과 주문 결과를 추적할 수 있는 고유 식별자를 유지한다.
- 주문 성공 여부를 HTTP 응답만으로 판단하지 않는다. 주문 접수/거부/부분체결/체결/취소 등 실제 주문 상태를 조회하고 시스템 상태와 대조한다.
- 프로그램 재시작 후에도 미체결 주문과 보유 포지션을 복구/대조할 수 있도록 설계한다.
- 전략 오류와 주문 시스템 오류를 분리하여 기록한다.
- 자동매매 코드는 수익을 보장하지 않으며, 백테스트 결과를 실거래 성과로 표현하지 않는다.

## 4. 한국투자증권 API 원칙
- KIS Open API의 공식 명세를 기준으로 구현한다.
- 국내주식/해외주식, 모의투자/실전투자의 API URL·TR ID·헤더·파라미터를 혼동하지 않도록 환경 설정으로 분리한다.
- 접근토큰은 필요한 범위에서 캐시하고 만료 시 재발급한다. 불필요한 토큰 발급을 반복하지 않는다.
- API 호출 제한과 응답 오류를 고려하여 재시도, backoff, timeout, rate limit 처리를 둔다.
- API 응답 원문에 의존하는 코드를 UI/전략 코드에 직접 흩뿌리지 않고 내부 데이터 모델로 변환한다.
- 시세, 주문, 체결, 잔고 등 데이터 종류별 API 클라이언트를 분리해 향후 교체와 테스트가 쉽도록 한다.
- KIS API 변경 가능성을 고려하여 외부 API 계층과 내부 도메인 로직을 느슨하게 결합한다.

## 5. 보안 및 인증정보
- App Key, App Secret, Access Token, 계좌번호, 개인 인증정보를 코드·문서·로그·GitHub에 절대 하드코딩하거나 커밋하지 않는다.
- 비밀값은 GitHub Actions Secrets, 서버/Worker 환경변수 등 안전한 비밀 저장소를 사용한다.
- 브라우저에 KIS Secret을 노출하지 않는다. 외부 API 호출에 Secret이 필요한 경우 서버/API Worker 계층에서 처리한다.
- 로그에는 인증정보, 전체 계좌번호, 민감한 요청 헤더를 출력하지 않는다.
- 테스트용 더미 계정/종목/데이터를 사용하고 실제 개인정보를 테스트 데이터로 저장하지 않는다.

## 6. 시스템 구조 원칙
권장 계층은 다음과 같다.

`Web UI → Backend/API Worker → KIS API Adapter → Domain/Trading Engine → Order/Risk/Position Services → Storage/Logs`

- UI는 전략 실행이나 KIS Secret을 직접 관리하지 않는다.
- KIS API Adapter는 외부 API 통신만 담당한다.
- Trading Engine은 전략 판단과 주문 의사결정을 담당한다.
- Risk Manager는 주문 가능 여부를 최종 차단할 수 있어야 한다.
- Order Manager는 주문 생성/전송/상태조회/취소/재동기화를 담당한다.
- Position/Account 서비스는 실제 잔고·체결과 내부 상태의 일치 여부를 관리한다.
- 데이터 공급자를 교체할 수 있도록 내부 표준 데이터 모델을 사용한다.

## 7. 데이터 처리 원칙
- 모든 시세에는 조회 시각, 데이터 기준 시각, 시장/거래소, 데이터 지연 여부를 가능한 경우 표시한다.
- 실시간 데이터와 지연 데이터, 장중 데이터와 장외 데이터를 구분한다.
- 가격·수량·금액 계산에서 부동소수점 오류가 주문에 영향을 주지 않도록 적절한 정밀도/정수 단위를 사용한다.
- 시장별 거래시간, 휴장일, 주문 가능 시간을 고려한다.
- 데이터 누락·지연·비정상 가격을 정상 시세로 취급하지 않는다.
- API 장애 시 오래된 데이터를 최신 데이터처럼 표시하지 않는다.

## 8. 전략 개발 원칙
- 전략 로직은 KIS API 호출 코드와 분리한다.
- 전략은 가능한 한 동일 입력에 동일 결과를 내도록 순수 함수/결정 로직으로 분리한다.
- 전략마다 진입 조건, 청산 조건, 포지션 제한, 주문 수량 산정, 위험 제한을 문서화한다.
- 백테스트/시뮬레이션과 실거래 실행 경로는 동일한 전략 인터페이스를 사용하되 주문 전송 계층은 분리한다.
- 백테스트에는 수수료, 세금, 슬리피지, 체결 실패/부분체결 등의 현실적 조건을 가능한 범위에서 반영한다.
- 전략 성과를 표시할 때 원금, 기간, 수수료/비용, 최대 낙폭 등 산출 기준을 명확히 한다.

## 9. UI 원칙
- 여러 종목을 한 화면에서 빠르게 비교할 수 있어야 한다.
- 현재가, 등락, 등락률, 거래량, 데이터 기준 시각 등 핵심 정보를 우선한다.
- 자동매매 상태는 `STOPPED / READY / RUNNING / ERROR / EMERGENCY_STOP`처럼 명확하게 구분한다.
- 실거래 여부를 화면에서 항상 눈에 띄게 구분한다.
- 주문 전송, 주문 접수, 체결, 거부, 취소 등의 상태를 사용자가 확인할 수 있어야 한다.
- API 오류나 데이터 지연을 성공 상태처럼 표현하지 않는다.
- 데스크톱을 우선하되 모바일에서도 핵심 상태 확인이 가능하도록 반응형으로 설계한다.

## 10. 문서 관리
- `agent.md`: AI/개발 작업의 공통 프로젝트 지침과 안전 규칙을 관리한다.
- `CLAUDE.md`: 검증 명령, 모듈 구조, 코드 작성 규칙 등 실무 규칙을 관리한다.
- `docs/status.md`: 현재 구현 상태, 최근 변경, 검증 결과, 남은 작업을 관리한다.
- `docs/roadmap.md`: 개발 우선순위, 단계, 완료 여부를 관리한다.
- `docs/design.md`: 시스템 구조, 모듈 배치, Worker 엔드포인트 계약, 데이터 모델, 설계 결정을 관리한다. Secret 값은 기록하지 않는다.
- `docs/quote-contract.md`: 시세 API 응답 계약과 stale/missing 정책을 관리한다.
- `docs/api-strategy.md`: 데이터 공급자 선정 근거를 관리한다.
- `docs/trading.md`: 전략 인터페이스, 구현 전략, 손절/익절, 사이징, 주문·Risk 규칙을 관리한다.
- `docs/operations.md`: 환경변수, 배포, 로컬 실행, 자동매매 운영, 장애 대응, 긴급 정지, 실계좌 활성화 체크리스트를 관리한다.
- 기능을 변경하면 관련 문서도 함께 갱신한다.
- 로드맵의 완료 표시는 실제 코드·검증 결과가 있는 경우에만 변경한다.

## 11. 테스트 및 검증
- 단위 테스트로 계산/전략/리스크/주문 상태 전이를 검증한다.
- KIS API를 직접 호출하는 테스트와 순수 로직 테스트를 분리한다.
- 기본 테스트는 실제 주문을 발생시키지 않아야 한다.
- API 장애, timeout, 잘못된 응답, 인증 만료, 부분체결, 중복 주문, 프로그램 재시작, 잔고 불일치 상황을 테스트한다.
- 배포 전 build/lint/test 등 프로젝트에 존재하는 검증 절차를 실행한다. Worker는 `npm run check`(타입체크)와 `npm test`를 모두 실행한다. vitest는 타입 오류를 잡지 못한다.
- 자동매매 관련 변경은 가능하면 모의투자 또는 dry-run에서 먼저 검증한다.

## 12. GitHub 작업 규칙
- 기본 기준 브랜치는 `main`이다.
- 작은 기능은 명확한 단위로 커밋한다.
- 문서 변경과 기능 변경은 가능한 한 구분한다.
- 대규모 변경이나 위험한 자동매매 기능은 작업 브랜치와 PR을 우선 사용한다.
- 커밋 직전 반드시 최신 `main` SHA를 재확인한다.
- 다른 변경이 들어온 경우 해당 변경을 보존하면서 다시 작업한다. 강제 덮어쓰기를 기본 동작으로 사용하지 않는다.
- 커밋 메시지는 `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:` 등의 일관된 형식을 사용한다.
- GitHub Actions 실패가 발생하면 원인을 확인하고 필요한 경우 수정 후 다시 검증한다.

## 13. 작업 진행 순서
1. 최신 `main` SHA 및 저장소 구조 확인
2. `agent.md`와 관련 설계/로드맵 확인
3. 현재 구현 상태와 미완료 작업 파악
4. 최소 범위의 설계 결정
5. 코드/문서 구현
6. 테스트 및 정적 검증
7. GitHub 반영 직전 `main` 최신 SHA 재확인
8. 변경사항 커밋
9. 배포/Actions 결과 확인
10. 관련 문서와 로드맵 상태 갱신
11. 다음 작업 후보와 남은 위험요소 기록

## 14. 완료 기준
기능을 완료로 표시하려면 다음을 만족해야 한다.
- 코드가 저장소에 반영됨
- 관련 문서가 최신 상태임
- 가능한 검증/테스트가 통과함
- 오류/예외 처리 확인
- 자동매매 기능은 실제 주문 발생 여부와 실행 모드를 명확히 확인함
- GitHub Actions 및 배포가 필요한 기능은 결과를 확인함
- 남은 제한사항과 후속 작업을 문서에 기록함

## 15. 기본 개발 단계
1. 프로젝트 지침 및 설계 문서 정리
2. KIS 인증/환경 구성과 API Adapter 기반 구축
3. 시세 조회 및 종목 대시보드
4. 계좌/잔고/보유종목 조회
5. 주문 모델 및 주문 상태 관리
6. DRY_RUN 주문 시뮬레이터
7. 리스크 관리 및 Kill Switch
8. 모의투자 주문 연동
9. 전략 엔진 및 백테스트
10. 실전 주문 연동(명시적 활성화와 안전장치 전제)
11. 모니터링·로그·알림·장애복구
12. 성능·보안·테스트·운영 안정화
