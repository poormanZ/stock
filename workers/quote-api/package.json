{
  "name": "stock-quote-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --var ALLOWED_ORIGIN:http://localhost:5173",
    "dev:paper": "wrangler dev --var ALLOWED_ORIGIN:http://localhost:5173 --var KIS_ENVIRONMENT:PAPER",
    "check": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260915.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4"
  }
}
