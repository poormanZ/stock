# 전략 · 주문 · 리스크 규칙

> 기준일: 2026-09-15

## 1. 전략 인터페이스

`workers/quote-api/src/strategy.ts`

```text
Strategy { id, params, warmupBars, evaluate(context) → Signal }
StrategyContext { symbol, candles(오래된 순, 마지막이 판단 봉), position | null }
Signal { action: 'buy' | 'sell' | 'hold', reason }
```

- 전략은 순수 함수다. KIS 호출, 상태 저장, 주문 전송을 하지 않는다.
- 백테스트(`backtest.ts`)와 자동 실행(`trading-engine.ts`)이 같은 `evaluate`를 호출한다.
- 전략 등록: `strategy.ts`의 `STRATEGIES`에 팩토리를 추가하면 `GET /strategies`, `/backtest`, `/trading/configure`에서 사용할 수 있다.

## 2. 구현된 전략

`GET /strategies`가 `details`로 아래 정보를 돌려준다.

### sma-crossover (기본)

| 항목 | 값 |
|---|---|
| 파라미터 | `fast`(기본 10), `slow`(기본 30), `trend`(기본 200, 0이면 필터 없음), `fast < slow` |
| 진입 | 단기 SMA가 장기 SMA를 상향 돌파하고, `trend>0`이면 종가가 trend일 SMA 위이며, 보유 포지션이 없을 때 매수 |
| 청산 | 단기 SMA가 장기 SMA를 하향 돌파하고 보유 포지션이 있을 때 매도 |
| 워밍업 | `max(slow, trend) + 1` 봉 |
| 사유 코드 | `SMA10>SMA30+T200`, `SMA10<SMA30`, `BELOW_TREND:200` |

### momentum

| 항목 | 값 |
|---|---|
| 파라미터 | `lookback`(기본 120), `trend`(기본 200) |
| 진입 | 종가가 lookback일 전 종가보다 높고(절대 모멘텀 양수) trend일 SMA 위이면 매수 |
| 청산 | 모멘텀이 0 이하로 떨어지거나 trend일 SMA 아래로 내려오면 매도 |
| 워밍업 | `max(lookback + 1, trend)` 봉 |
| 사유 코드 | `MOMENTUM_UP:12.3%`, `MOMENTUM_DOWN:-1.2%`, `BELOW_TREND:200` |

### volatility-breakout

| 항목 | 값 |
|---|---|
| 파라미터 | `k`(기본 0.5, ≤2), `trend`(기본 0) |
| 진입 | 당일 가격이 `시가 + k × 전일(고가−저가)`를 넘으면 매수. 장중(`intraday`)에는 현재가로, 일봉 백테스트에서는 고가로 판단해 종가를 미리 아는 편향을 막는다 |
| 청산 | 진입 다음 거래일 시가에 매도(`entryDate < 당일`) |
| 워밍업 | `max(2, trend)` 봉 |
| 비고 | 백테스트에서 거래비용(왕복 약 0.4%)에 잠식되어 전 구간 손실. 선택지로만 남기고 대시보드 프리셋에서는 제외 |

## 3. 손절 / 익절 / 트레일링 스탑 (`checkExitRules`)

전략 신호보다 먼저 평가한다. 손절·익절은 평균단가 대비 현재가 변동률(%), 트레일링 스탑은 진입일 이후 최고 종가(현재가 포함) 대비 하락률로 판단한다. 트레일링 스탑은 포지션에 `entryDate`가 있어야 동작하며, 엔진은 마지막 매수 체결 주문의 KST 날짜를 사용한다.

| 규칙 | 기본값 | 0이면 |
|---|---|---|
| `stopLossPct` | 7 | 사용 안 함 |
| `takeProfitPct` | 0 | 사용 안 함 |
| `trailingStopPct` | 10 | 사용 안 함 |

고정 익절을 기본에서 뺀 이유는 §9의 비교 결과다.

## 4. 포지션 사이징 (`sizeEntry`)

진입 수량 = min(현금 × `cashFraction` ÷ 유효가, `maxOrderAmount` ÷ 가격, `maxOrderQuantity`, `maxPositionQuantity` − 보유수량)을 정수로 내림. 유효가는 가격 × (1 + 슬리피지 + 수수료)다.

| 규칙 | 기본값 |
|---|---|
| `cashFraction` | 0.2 |
| `maxOrderAmount` | 1,000,000원 |
| `maxOrderQuantity` | 1,000주 |
| `maxPositionQuantity` | 5,000주 |

매도는 항상 보유 수량 전체다.

## 5. 백테스트 (`POST /backtest`)

- 봉 i 종가에서 신호를 내고 봉 i+1 시가에 시장가로 체결한다(미래 참조 방지).
- 체결·수수료(15bp)·거래세(매도 20bp)·슬리피지(5bp)는 DRY_RUN 시뮬레이터(`simulateOrder`)를 그대로 사용한다.
- 신호에 `price`가 있으면(돌파 가격, 당일 시가) 그 봉 안에서 고가·저가 범위로 제한한 가격에 즉시 체결한다.
- 지표: 총수익률, 거래 수, 승률, 손익비(총이익/총손실), 최대낙폭, 총수수료·세금, 같은 구간 단순보유 수익률(`buyAndHoldReturnPct`). 미청산 포지션은 마지막 종가로 평가한다.
- 최대 범위 1,000일. 결과는 시뮬레이션이며 실거래 성과를 보장하지 않는다(응답에 `disclaimer` 포함).

## 6. 주문 규칙

| 항목 | 규칙 |
|---|---|
| 요청 검증 | `id`, `clientOrderId`, 6자리 `symbol`, `side ∈ buy/sell`, `orderType ∈ market/limit`, 정수 `quantity > 0`, limit은 `limitPrice > 0`, market은 `limitPrice` 금지, `reason`은 선택(≤200자) |
| 주문 사유 | 자동매매는 신호 사유(`SMA5>SMA20`, `STOP_LOSS:-5.20%`, `TAKE_PROFIT:+10.10%`)를, 대시보드 수동 주문은 `manual`을 `reason`에 넣는다. 주문 기록·감사 로그·실행 이력에 남고 KIS로는 전송하지 않는다 |
| 상태 변경 | `transitionOrder`만 사용. 허용 전이는 `docs/design.md` §7 |
| 멱등성 | 같은 `clientOrderId`는 기존 주문을 반환한다(DRY_RUN·PAPER·LIVE). 자동매매는 `AUTO-{mode}-{날짜}-{종목}-{방향}` |
| 재시도 | 주문 POST는 KIS 5xx에 재시도하지 않는다. 429만 재시도. 결과 불확실 시 `UNKNOWN` |
| 취소 | `SUBMITTED/ACCEPTED/PARTIALLY_FILLED`만 취소 가능. 접수 응답으로 `CANCELED` 기록, 이후 resync로 대조 |
| DRY_RUN | KIS 계좌·시세·장시간에 의존하지 않는다. `referencePrice` 사용 |
| PAPER | 정규장(KST 09:00~15:30 평일)만. reconciliation Gate·실시간 시세 필수 |
| LIVE | `docs/operations.md` §8 게이트 통과 시에만. 스케줄러 미지원 |

## 7. Risk Manager (`checkRisk`)

검사 순서(하나라도 실패하면 차단):

1. Kill Switch
2. reconciliation 불일치(PAPER/LIVE)
3. API 장애 플래그
4. 기준가 유효성
5. 시세 나이 ≤ `maxQuoteAgeMs`(15초). 기준 시각은 KIS 체결시각, 없으면 수신 시각
6. `maxOrderQuantity` 1,000주
7. `maxOrderAmount` 1,000,000원(지정가는 지정가×수량, 시장가는 기준가×수량)
8. `maxPositionQuantity` 5,000주, 매도 후 음수 금지
9. `maxDailyOrders` 20건(KST 당일 주문 기록 수)
10. `maxDailyLoss` 100,000원(당일 실현손실. DRY_RUN은 시뮬레이터, PAPER/LIVE는 체결 증분 원장)

한도는 `DEFAULT_RISK_CONFIG`에 고정되어 있다. 계좌 규모에 맞게 바꾸려면 코드 수정과 테스트 갱신이 필요하다.

## 8. 포지션 원장과 실현손익

- 내부 포지션은 주문 기록의 체결 증분(`executedQuantity` 차이)으로만 갱신한다(`position-ledger.ts`). 같은 기록을 다시 적용해도 변화가 없다.
- 기준선은 `POST /paper/position-sync`로 KIS 스냅샷을 채택해 만든다.
- 매도 체결 시 (체결가 − 평균단가) × 수량을 KST 당일 실현손익에 더한다. 날짜가 바뀌면 0부터 시작한다.
- DRY_RUN은 수수료·세금까지 뺀 값을 실현손익으로 기록한다.

## 9. 전략 비교 (2026-09-15)

관심종목 6개(005930, 000660, 005380, 035420, 035720, 068270)의 KIS 일봉 2024-01-02~2026-09-15를 받아 200봉 워밍업 후 2024-10-29~2026-09-15 구간에서 비교했다. 현금 100%, 수수료 0.15%·거래세 0.20%·슬리피지 0.05% 반영, 한 종목씩 단독 백테스트. 전반/후반은 구간을 반으로 나눈 것이다.

| 전략 | 전체 평균 | 전반 | 후반 | 평균 MDD | 거래 | 승률 | 판단 |
|---|---:|---:|---:|---:|---:|---:|---|
| SMA 5/20, 손절5/익절10 (이전 기본) | +5.3% | +6.1% | +7.2% | 28.8% | 85 | 34% | 익절 10%가 추세를 잘라 수익이 거의 없음 |
| SMA 5/20, 손절5/트레일링8 | +89.4% | +27.2% | +30.9% | 36.3% | 85 | 31% | 진입 동일, 청산만 바꿔도 크게 개선 |
| **SMA 10/30 + 200일 필터, 손절7/트레일링10 (새 기본)** | **+94.7%** | **+22.3%** | **+35.9%** | **27.6%** | 35 | 43% | 세 구간 모두 수익률−MDD 양수. 거래 수 절반, MDD 최저권 |
| SMA 20/60 + 200일 필터, 손절7/트레일링10 | +25.8% | +20.2% | −0.8% | 20.4% | 16 | 50% | 낙폭 작지만 후반 무수익 |
| 모멘텀 120/200, 스탑 없음 | +113.2% | +2.8% | +88.7% | 46.1% | 53 | 19% | 후반 편중, 승률 낮음 |
| 모멘텀 120/200, 손절7/트레일링12 | +57.7% | +2.2% | +47.0% | 50.3% | 104 | 27% | 스탑이 잦은 재진입을 유발 |
| 변동성 돌파 k0.5, 손절3 | −58.1% | −23.8% | −44.0% | 63.1% | 864 | 40% | 1일 보유 + 거래비용으로 부적합 |
| 변동성 돌파 k0.5 + 5일선 필터 | +38.2% | +25.4% | +13.1% | 27.9% | 596 | 52% | 필터로 개선되나 거래 과다 |
| 단순 보유 | +198.4% | +50.2% | +80.1% | — | — | — | 강세장(000660 +773%) 기준선 |

해석과 한계:

- 같은 진입에서 고정 익절을 트레일링 스탑으로 바꾸는 것이 가장 큰 개선이었다. 추세추종 전략에서 익절은 큰 수익을 잘라낸다.
- 200일 추세 필터는 거래 수를 85→35건으로 줄이고 승률·MDD를 개선했다. 최근 시스템 트레이딩 자료에서도 기간 튜닝보다 추세/레짐 필터 추가를 우선한다.
- 어떤 전략도 이 구간의 단순 보유를 넘지 못했다. 2024~2026은 반도체 대형주 강세장이어서 추세추종+손절은 낙폭을 줄이는 대가로 수익을 일부 포기한다. 하락장·횡보장 구간이 표본에 부족하다.
- 표본은 6종목 2년이며 후보는 11개 조합에서 골랐다. 선택 편향이 있으므로 이 결과는 "이전 기본이 부적절했다"는 근거로는 충분하지만 미래 수익을 보장하지 않는다. PAPER 안정성 검증 기간에 실측을 다시 본다.
- 재현: `GET /candles`로 일봉을 받은 뒤 `runBacktest`를 전략별로 호출한다(`POST /backtest`로 종목·전략 단위 확인 가능).
